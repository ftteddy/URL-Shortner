<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortner</title>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap');


        /* Reset styles to avoid browser inconsistencies */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html,
        body {
            height: 100%;
        }

        body {
            font-family: "Poppins", system-ui;
            font-weight: 100;
            font-style: normal;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f4f4f4;
        }

        form {
            position: relative;
            display: flex;
            align-items: center;
            gap: 10px;
            flex-wrap: wrap;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        input,
        button {
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        input {
            flex: 1;
            min-width: 150px;
        }

        button {
            flex: 0 0 auto;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
        }

        .urlTAG {
            margin-right: 20px;
        }

        .login {
            flex: 0 0 auto;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            size: 80px;
        }

        .singup {
            flex: 0 0 auto;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            size: 80px;
        }

        .formType {
            margin: 0px;
            position: absolute;
            bottom: 690px;
            right: 10px;
            padding: 0px;
        }

        .urlGnrated {
            color: black;
            font-weight: 400;
        }

        .formType {
            margin: 0px;
            position: absolute;
            bottom: 690px;
            right: 10px;
            padding: 0px;
        }
        .formTypeLogin{
            margin: 0px;
            position: absolute;
            bottom: 690px;
            right: 99px;
            padding: 0px;
        }
        .login{
            flex: 0 0 auto;
            cursor: pointer;
            background-color: white;
            color: black;
            border: none;
            padding: 10px 20px;
        }
    </style>
</head>

<body>
    <h1 class="urlTAG">URL SHORTNER</h1>
    <div>
        <form class="formType" method="GET" action="/login">
            <button class="login" type="submit">Login</button>
        </form>
        <form class="formType formTypeLogin" method="GET" action="/singup">
            <button class="singup" type="submit">Singup</button>
        </form>
        
        </form>

        <form method="POST" action="/url">

            <input type="text" name="ejsUrl" placeholder="https://www.exampal.com" />
            <button type="submit">Genrate</button>

            <% if (locals.url) { %>
                <a class="urlGnrated" href="http://localhost:7000/url/<%=url%>">URL Genrated: http://localhost:7000/url/
                    <%=url%></a>
                <% } %>

        </form>
    </div>
</body>

</html>